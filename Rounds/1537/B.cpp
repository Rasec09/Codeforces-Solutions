#include <bits/stdc++.h>
using namespace std;
#define optimizar_io ios_base::sync_with_stdio(false);cin.tie(0);

int main() {
	optimizar_io
	int tc, n, m, x, y;

	cin >> tc;
	while (tc--) {
		cin >> n >> m >> x >> y;
		cout << 1 << " " << 1 << " " << n << " " << m << "\n";
	}
	return 0;
}
